# AGENTS.md

本文件用于指导 AI 编码代理在本仓库中工作。执行任务时优先遵循本文件，其次参考 `README.md`、`READPROJECT.md`、`RULES.md` 等文档；如果文档与实际代码不一致，以实际代码为准，并在合适时更新相关文档。

## 项目概况

- 项目名称：`yc-page`
- 技术栈：Java 21、Spring Boot 3.5.16、Maven、MyBatis Plus、MySQL、Redis、JWT、Lombok、Validation、AOP。
- 主包路径：`src/main/java/ikun/yc/ycpage`
- 资源路径：`src/main/resources`
- 测试路径：`src/test/java`

## 常用命令

- 运行测试：`mvn test`
- 打包项目：`mvn package`
- 启动项目：`mvn spring-boot:run`

执行命令前先确认本机 Java、Maven、数据库、Redis 与 `src/main/resources/application.properties` 配置匹配。不要把本地密钥、账号、Token、数据库密码等敏感信息写入文档或提交内容。

## 代码风格

- 生成或修改代码必须使用 UTF-8 编码。`pom.xml` 已配置 `project.build.sourceEncoding`、`project.reporting.outputEncoding` 和编译器 `encoding` 为 `UTF-8`。
- 优先遵循项目现有风格、架构和测试方式；新增 Controller、Service、Mapper、Entity、DTO、枚举、AOP、配置类时，先参考同目录已有实现。
- 保持 Spring Boot 分层结构清晰：Controller 负责接口入参和返回，Service 负责业务逻辑，Mapper 负责数据访问，Entity/DTO 负责数据结构。
- 使用项目已有的统一返回结构、异常类型、注解、AOP、缓存和上下文工具；不要为单次需求引入新的框架或抽象。
- 修改 MyBatis XML、枚举、实体字段或数据库相关逻辑时，同步检查 Mapper、Service、Controller、校验注解和文档说明。

## 注释要求

- 编写类或方法都要写注释。Java 类和公开方法优先使用 JavaDoc，说明职责、关键参数、返回值和异常场景。
- 生成的变量也要在变量尾部加注释，说明变量用途，例如：`String token = jwtUtils.generateToken(userId); // 登录令牌`
- 注释应解释业务意图、约束或非显然逻辑，避免写没有信息量的重复说明。
- 现有代码没有注释时，新增代码仍需满足本文件要求；修改旧代码时尽量只补充与本次变更直接相关的注释。

## 编码与中文检查

- 写入文件后必须检查中文内容，确认中文没有全部变成问号，也没有被转换成反斜杠加 `u` 和十六进制数字组成的 Unicode 转义形式。
- 对 Markdown、Java、XML、properties、JSON 等文件都按 UTF-8 处理；读取中文异常时，优先怀疑终端显示编码，而不是直接改写文件。
- 如果必须处理已有乱码，先确认原始字节和预期编码，再做最小范围修复。

## URL 与外部资料

- 提供 URL 链接前，必须先验证链接有效性。优先通过浏览器、HTTP 状态码或官方来源确认链接可访问。
- 引用外部资料时，优先选择官方文档、源码仓库、发布说明或项目维护者提供的文档。
- 不要把未经验证的链接写入项目文档；如果链接失效，说明情况并寻找可靠替代来源。

## 变更原则

- 只修改完成用户需求所必需的文件和代码。
- 不顺手重构无关代码，不统一格式化无关文件，不删除与本次任务无关的已有内容。
- 如果发现无关问题，可以在回复中说明，但不要擅自扩大修改范围。
- 保持每一处改动都能对应到明确需求、缺陷修复或验证结果。

## 测试与验证

- 代码变更后优先运行与变更范围匹配的测试；范围不明确时运行 `mvn test`。
- 涉及接口行为时，补充或更新对应测试，并检查成功、失败和边界输入。
- 涉及缓存、鉴权、拦截器、AOP、数据库字段、枚举转换时，要额外验证相关调用链。
- 如果因为环境缺少数据库、Redis、外部服务或凭据导致测试无法完成，需要在最终说明中明确未验证项和原因。

## 文档维护

- 如果这些文档与实际代码不一致，以实际代码为准，并在合适时更新文档。
- 更新文档时保持中文可读、结构清晰、示例可运行。
- 文档中的命令、路径、接口、配置项和链接应与当前仓库实际内容一致。

## 附录：通用代理准则

以下内容翻译自已验证可访问的 `https://raw.githubusercontent.com/forrestchang/andrej-karpathy-skills/main/CLAUDE.md`，用于补充本项目的代理行为准则。

用于减少大语言模型在编码中常见错误的行为准则。可按需要与项目专属说明合并。

取舍：这些准则更偏向谨慎而不是速度。对于简单任务，应结合实际情况判断。

### 1. 编码前先思考

不要假设。不要隐藏困惑。要说清取舍。

实现之前：

- 明确说明你的假设。如果不确定，就提问。
- 如果存在多种理解方式，把它们列出来，不要沉默地自行选择。
- 如果有更简单的方案，要说出来。必要时提出不同意见。
- 如果某件事不清楚，就停下来。指出困惑在哪里，然后提问。

### 2. 简洁优先

用能解决问题的最少代码。不要做猜测性的扩展。

- 不添加需求之外的功能。
- 不为只用一次的代码创建抽象。
- 不添加用户没有要求的灵活性或可配置性。
- 不为不可能出现的场景编写错误处理。
- 如果你写了 200 行，而本可以用 50 行完成，就重写。

问问自己：“资深工程师会不会认为这过度复杂？”如果答案是会，就简化。

### 3. 外科手术式修改

只触碰必须修改的内容。只清理自己造成的问题。

编辑现有代码时：

- 不要顺手“改进”相邻代码、注释或格式。
- 不要重构没有问题的内容。
- 匹配现有风格，即使你会用不同方式实现。
- 如果发现无关的废弃代码，可以提出来，但不要删除。

当你的修改产生孤立内容时：

- 移除由你的修改导致不再使用的 import、变量或函数。
- 除非被明确要求，不要移除原本就存在的废弃代码。

检验标准：每一行变更都应能直接追溯到用户的请求。

### 4. 目标驱动执行

定义成功标准。循环验证直到完成。

把任务转化为可验证的目标：

- “添加校验” 转化为 “为无效输入编写测试，然后让测试通过”
- “修复这个 bug” 转化为 “编写能复现问题的测试，然后让测试通过”
- “重构 X” 转化为 “确保重构前后测试都通过”

对于多步骤任务，说明一个简短计划：

```text
1. [步骤] -> 验证：[检查项]
2. [步骤] -> 验证：[检查项]
3. [步骤] -> 验证：[检查项]
```

清晰的成功标准能让你独立循环推进。模糊的标准，例如“让它能用”，通常需要反复澄清。

如果这些准则生效，表现应是：差异中不必要的修改更少，因为过度复杂导致的重写更少，澄清问题发生在实现之前，而不是错误发生之后。
