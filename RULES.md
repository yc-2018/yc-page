# yc-page Rules

## 1. 目标

本规则用于约束 `yc-page` 项目的后续开发、维护与 AI 协作行为。  
核心原则：

- 保持现有 Spring Boot + MyBatis-Plus + Redis + JWT 体系不被破坏
- 优先延续现有项目风格，而不是强行引入全新范式
- 所有新增功能都要围绕“个人工具后端”定位展开
- 修改时优先保证兼容现有接口与数据结构

---

## 2. 项目定位规则

- 本项目是个人主页/个人工具箱后端，不是通用中台
- 新功能应优先服务以下现有业务域：
  - 用户与登录
  - 书签管理
  - 备忘录/待办
  - 搜索引擎配置
  - 微信公众号能力
  - 微信小程序能力
  - 个人统计类能力
- 如果新增功能明显不属于以上范围，先评估是否真的应该进入本仓库

---

## 3. 技术栈规则

- JDK 版本按当前项目保持 `Java 8`
- Spring Boot 版本按当前项目保持 `2.7.5`
- 数据访问层继续使用 `MyBatis-Plus`
- 缓存继续使用 `Redis`
- 鉴权继续使用 `JWT + LoginInterceptor + BaseContext`
- 不要随意引入与现有体系冲突的重型框架
- 没有充分理由，不引入 Spring Security 全家桶替换现有鉴权体系

---

## 4. 分层规则

- `controller` 负责接收请求、参数校验、组织返回值
- `service` / `service.impl` 负责业务逻辑
- `mapper` 负责数据库访问
- `entity` 负责实体、DTO、枚举
- `common` 放通用注解、切面、异常、上下文、工具类
- `config` 只放配置相关代码

补充约束：

- 新增复杂业务逻辑，不要直接堆在 Controller
- 简单 CRUD 可延续当前项目风格，但复杂逻辑仍应进入 Service
- 当前项目存在 MyBatis-Plus ActiveRecord 写法，新增代码时优先“跟随所在模块已有风格”
- 不要在同一模块里混用三四种完全不同的数据访问方式

---

## 5. 认证与用户上下文规则

- 默认所有接口都视为需要登录
- 只有明确公开接口，才能加 `@PassToken`
- 当前登录用户统一从 `BaseContext.getCurrentId()` 获取
- 不信任前端传入的 `userId`
- 需要给实体自动注入用户 ID 时，优先使用 `@UserId`
- 新增接口如涉及用户私有数据，查询和更新条件必须带当前用户限制

禁止：

- 通过请求参数直接决定要操作哪个用户的数据
- 绕过 `LoginInterceptor` 自行做一套平行登录逻辑

---

## 6. 接口设计规则

- 返回值统一使用 `R<T>`
- 成功返回使用 `R.success(...)`
- 失败返回使用 `R.error(...)`
- 接口路径命名保持与现有风格一致，避免无意义缩写
- 尽量保持 REST 风格，除非模块已有历史风格需要兼容
- 修改已有接口时优先保持向后兼容

新增接口前必须检查：

- 是否与现有路径冲突
- 是否与现有语义重复
- 是否需要登录
- 是否需要缓存
- 是否需要限流
- 是否需要日志记录

---

## 7. 参数校验规则

- Controller 层优先做基础参数校验
- 可用 `@Valid`、`@Pattern`、`@NotNull` 等校验注解时，优先使用
- 业务语义校验放到 Service 或实体辅助方法中
- 非法参数统一抛出项目已有异常，交给全局异常处理器

---

## 8. AOP 使用规则

### 8.1 `@CountControl`

- 对新增、修改、删除类接口，优先评估是否需要限流
- 对高频调用、易滥用接口，应加 `@CountControl`
- 频控阈值要结合业务实际，不要照搬

### 8.2 `@RedisCache`

- 仅用于结果稳定、读取频繁、用户维度清晰的查询接口
- 缓存 key 设计必须保持“用户隔离”
- 返回结果过大、参数复杂或变化频繁的接口慎用缓存

### 8.3 `@DelCache`

- 任何会影响缓存结果的写操作，都要考虑补充 `@DelCache`
- 缓存与写接口必须成对考虑，避免读到旧数据

### 8.4 `@Log`

- 重要新增、修改、删除行为优先加 `@Log`
- 不要给纯高频无价值接口滥加日志，避免日志表膨胀

---

## 9. 数据库与实体规则

- 实体字段命名、表命名延续现有数据库结构
- 时间字段优先复用现有 `createTime` / `updateTime` 自动填充能力
- 不要轻易改动已有表字段语义
- 涉及逻辑删除时，优先遵循当前模块已有实现方式

补充：

- 当前项目不同模块的“删除”策略不完全一致，新增代码要跟随所属模块风格
- 如果某模块已采用软删除，不要突然改成物理删除
- 如果需要新增表，命名风格与现有表保持一致，避免风格割裂

---

## 10. 排序字段规则

本项目中有一类重要历史设计：`id/id/id` 字符串排序方案。  
包括但不限于：

- 书签排序
- 搜索引擎排序
- 用户首页链接排序

规则：

- 涉及这些模块时，必须先理解排序字段来源与归属表
- 修改排序逻辑时，必须同步考虑新增、删除、拖拽、分类切换等场景
- 不要局部改一处而忽略其它更新入口
- 除非明确做整体重构，否则不要半途引入新的排序存储方案

---

## 11. 书签模块规则

- `Bookmarks` 的 `type` 语义必须保持稳定
- 新增/删除书签时，要同步维护分组或根节点的 `sort`
- 所有书签数据必须与当前用户绑定
- 涉及拖拽排序时，要先校验前端传入排序结果与数据库当前数据是否一致

---

## 12. 备忘录模块规则

- `memo` 是核心业务表，修改前先评估对查询、统计、循环事项的影响
- `itemType` 语义必须与 `MemoType` 保持一致
- `completed` 字段当前承载完成态和逻辑删除态，修改时必须谨慎
- 更新待办时要继续遵守 `Memo.toReviseInfo()` 的安全约束思路
- 新增与循环备忘录相关功能时，要同步考虑 `loop_memo_item` 和 `number_of_recurrences`

---

## 13. 搜索引擎模块规则

- 搜索引擎数据与排序数据分离存储，修改时必须同时考虑两部分
- `LinkType` 是核心枚举，新增类型前要检查：
  - 枚举定义
  - 参数转换器
  - `UserConfig` 对应字段
  - 排序读写逻辑
- 新用户初始化搜索引擎时，继续使用 `initial-engines.json` 机制

---

## 14. 微信相关规则

### 14.1 微信公众号

- `/接收微信接口` 是线上接入点，修改时必须谨慎
- 微信消息 XML 解析逻辑不要随意破坏
- 登录验证码逻辑依赖 Redis，有效期和 key 设计要保持稳定

### 14.2 微信小程序

- 小程序登录以 `openid` 作为用户唯一标识
- 不要把网页端用户体系和小程序用户体系强行混成一套表
- 涉及打卡功能时，必须保留当前“同位置同日不可重复”的业务约束，除非有明确需求变更

---

## 15. 安全规则

- 所有密钥、Token、数据库密码、微信配置都必须走环境变量
- 禁止把真实密钥硬编码进代码
- 涉及外部 HTTP 调用时，要考虑失败兜底与异常处理
- `OtherController` 下的 `/other/run-script` 属于高风险接口，非必要不要扩展类似能力
- 如果必须新增运维类接口，必须至少补充权限、限流、调用边界控制

---

## 16. 路由冲突规则

- 新增 Controller 前必须检查是否与现有 `@RequestMapping` 冲突
- 当前 `MiniAccountMemoController` 使用了 `/memo`，与 `MemoController` 存在冲突风险
- 该模块后续开发时必须先重新规划路径，例如单独使用 `/mini/accountMemo` 或其它独立前缀

---

## 17. 测试与验证规则

- 项目当前测试较弱，新增核心逻辑时至少要做基本验证
- 即使不补全自动化测试，也必须做最小化手工验证清单
- 涉及以下改动时，优先补测试：
  - 登录鉴权
  - 频控
  - 缓存
  - 排序
  - 统计聚合
  - 微信登录

---

## 18. 文档规则

- 修改核心模块后，视情况同步更新 `READPROJECT.md`
- 如果新增完整业务模块，建议补充独立说明文档
- 对外暴露的重要接口变更，应记录请求方式、路径、参数、登录要求、返回结构

---

## 19. AI / 协作开发规则

- 在修改代码前，先判断改动属于哪个业务域
- 先复用项目已有注解、切面和工具类，不重复造轮子
- 不要用“理想化重构”破坏当前项目运行方式
- 如果发现历史设计不优雅，优先兼容式优化，而不是全量推翻
- 改动前先检查是否会影响：
  - 鉴权
  - 缓存
  - 限流
  - 日志
  - 排序字段
  - 用户隔离

---

## 20. 默认执行准则

以后在本项目中写代码，默认遵守以下执行顺序：

1. 先确认功能属于哪个现有模块
2. 再确认是否需要登录、限流、日志、缓存
3. 再确认数据是否必须按用户隔离
4. 再确认是否影响排序字段或统计字段
5. 最后再动代码

如果以上任一项没有想清楚，先不要直接改。
